<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
<title>Maintenance Mode</title>

<style>
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: "Segoe UI", sans-serif;
}

html, body {
  width: 100%;
  height: 100%;
  overflow: hidden; /* ✅ FIX */
}

body {
  background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
  display: flex;
  align-items: center;
  justify-content: center;
  color: #fff;
  position: relative;
}

.container {
  text-align: center;
  padding: 40px;
  background: rgba(255, 255, 255, 0.08);
  border-radius: 20px;
  backdrop-filter: blur(12px);
  animation: fadeIn 1.5s ease;
  max-width: 500px;
  width: 90%;
  z-index: 2;
}

h1 {
  font-size: 2.3rem;
  margin-bottom: 15px;
}

p {
  font-size: 1.05rem;
  margin-bottom: 25px;
  opacity: 0.9;
}

.emoji-box {
  margin-bottom: 25px;
}

.emoji {
  font-size: 2.6rem;
  margin: 0 10px;
  display: inline-block;
  animation: float 3s ease-in-out infinite;
}

.emoji:nth-child(2) { animation-delay: .4s; }
.emoji:nth-child(3) { animation-delay: .8s; }
.emoji:nth-child(4) { animation-delay: 1.2s; }

.loader {
  width: 100%;
  height: 12px;
  background: rgba(255,255,255,0.25);
  border-radius: 10px;
  overflow: hidden;
  margin-bottom: 15px;
}

.progress {
  height: 100%;
  width: 0%;
  background: linear-gradient(90deg, #00f260, #0575e6);
  animation: loading 5s infinite;
}

.small {
  font-size: 0.9rem;
  opacity: 0.75;
}

/* Floating background emojis */
.bg-emoji {
  position: absolute;
  top: 100%;
  font-size: 2rem;
  opacity: 0.15;
  animation: rise linear infinite;
  pointer-events: none;
}

@keyframes float {
  0%,100% { transform: translateY(0); }
  50% { transform: translateY(-15px); }
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(30px); }
  to { opacity: 1; transform: translateY(0); }
}

@keyframes loading {
  0% { width: 0%; }
  100% { width: 100%; }
}

@keyframes rise {
  from {
    transform: translateY(0) rotate(0deg);
  }
  to {
    transform: translateY(-120vh) rotate(360deg);
  }
}
</style>
</head>

<body>

<div class="container">
  <div class="emoji-box">
    <span class="emoji">⚙️</span>
    <span class="emoji">🛠️</span>
    <span class="emoji">💻</span>
    <span class="emoji">🚧</span>
  </div>

  <h1>We’re Under Maintenance</h1>
  <p>Our website is getting a fresh upgrade.<br>Please check back soon!</p>

  <div class="loader">
    <div class="progress"></div>
  </div>

  <p class="small">Thanks for your patience 💙</p>
</div>

<script>
const emojis = ["⚙️","🛠️","🚧","💡","🔧"];
const total = 12;

for (let i = 0; i < total; i++) {
  const span = document.createElement("span");
  span.className = "bg-emoji";
  span.textContent = emojis[Math.floor(Math.random() * emojis.length)];
  span.style.left = Math.random() * 100 + "vw";
  span.style.animationDuration = (8 + Math.random() * 6) + "s";
  span.style.fontSize = (1.5 + Math.random() * 1.5) + "rem";
  document.body.appendChild(span);
}
</script>

</body>
</html>
